import {NgDocPage} from '@ng-doc/core';
import OnboardingProcessCategory from '../ng-doc.category';

const FrontendOnboardingPage: NgDocPage = {
	title: `Frontend Onboarding`,
	mdFile: './index.md',
	category: OnboardingProcessCategory,
	order: 2
};

export default FrontendOnboardingPage;
